# Examples

The examples are intentionally separate Maven projects. They do not participate
in the root reactor, so the library remains a single Java 8 annotation-processor
artifact.

- [Maven warning and strict-mode demo](maven/README.md)
